repos:
- repo: local
  hooks:
  - id: terraform-fmt
    name: Terraform fmt check (terraform/)
    entry: terraform fmt -check -recursive terraform
    language: system
    pass_filenames: false
    files: ^terraform/.*\.tf$
