repos:
- repo: local
  hooks:
  - id: terraform-fmt
    name: Terraform fmt (terraform/)
    entry: terraform fmt -recursive terraform
    language: system
    pass_filenames: false
    files: ^terraform/.*\.tf$
