# Copy this file to .env.local for local testing.
# Vite only exposes variables prefixed with VITE_.

# Local default mode (recommended for normal local development)
VITE_APP_ENV=local
VITE_RESUME_MODE=local-static
VITE_RESUME_API_URL=
VITE_RESUME_OBJECT_KEY=private-downloads/resume/zhenwei-seo-cv.pdf
VITE_RESUME_EXPIRY_SECONDS=300
